apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "readinglevel.fullname" . }}-test-connection"
  labels:
{{ include "readinglevel.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "readinglevel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
